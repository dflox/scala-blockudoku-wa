GET         /                          controllers.HomeController.index()
POST        /direction/:dir            controllers.HomeController.direction(dir: String)
POST        /select/:ind               controllers.HomeController.selectElement(ind: Int)

POST        /place/:tileIndex          controllers.HomeController.placeElement(tileIndex: Int)
GET         /assets/*file              controllers.Assets.versioned(path="/public", file: Asset)
POST        /preview/:tileIndex        controllers.HomeController.getPreview(tileIndex: Int)

GET     /assets/*file     controllers.Assets.versioned(path="/public", file: Asset)

